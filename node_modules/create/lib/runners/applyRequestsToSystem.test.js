import { describe, it, vi } from "vitest";
describe("applyRequestsToSystem", () => {
    it("sends all requests in parallel", () => {
        const send = vi.fn();
        const first = { id: "first", send: firstSend };
        const second = { id: "first", send: secondSend };
    });
});
//# sourceMappingURL=applyRequestsToSystem.test.js.map
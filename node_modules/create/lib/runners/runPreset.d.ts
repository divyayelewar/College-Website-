import { ProductionMode } from "../modes/types.js";
import { AnyShape, InferredObject } from "../options.js";
import { Preset } from "../types/presets.js";
import { NativeSystem } from "../types/system.js";
import { PromiseOrSync } from "../utils/promises.js";
export interface AugmentingPresetRunSettings<OptionsShape extends AnyShape> extends RunSettingsBase {
    options?: Partial<InferredObject<OptionsShape>>;
    optionsAugment: (options: Partial<InferredObject<OptionsShape>>) => PromiseOrSync<InferredObject<OptionsShape>>;
}
export interface FullPresetRunSettings<OptionsShape extends AnyShape> extends RunSettingsBase {
    options: InferredObject<OptionsShape>;
    optionsAugment?: (options: InferredObject<OptionsShape>) => Promise<Partial<InferredObject<OptionsShape>>>;
}
export interface RunSettingsBase extends Partial<NativeSystem> {
    directory?: string;
    mode?: ProductionMode;
}
export declare function runPreset<OptionsShape extends AnyShape>(preset: Preset<OptionsShape>, settings: AugmentingPresetRunSettings<OptionsShape> | FullPresetRunSettings<OptionsShape>): Promise<void>;

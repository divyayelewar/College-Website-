import { createSystemContext } from "../system/createSystemContext.js";
import { awaitLazyProperties, } from "../utils/awaitLazyProperties.js";
export async function produceBase(base, settings) {
    if (!base.produce) {
        return settings.options;
    }
    const system = createSystemContext({
        directory: settings.directory ?? ".",
        ...settings,
    });
    return await awaitLazyProperties(base.produce({
        options: settings.options ?? {},
        ...system,
    }));
}
//# sourceMappingURL=produceBase.js.map
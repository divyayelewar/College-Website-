export function isTemplate(value) {
    return (!!value &&
        typeof value === "object" &&
        "presets" in value &&
        Array.isArray(value.presets));
}
//# sourceMappingURL=utils.js.map
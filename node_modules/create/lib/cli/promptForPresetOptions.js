import readline from "node:readline/promises";
import { promptForSchema } from "./promptForSchema.js";
export async function promptForPresetOptions(bases, existing) {
    const options = {};
    const rl = readline.createInterface({
        input: process.stdin,
        output: process.stdout,
    });
    for (const [key, base] of Object.entries(bases)) {
        if (!base.isOptional()) {
            options[key] = existing[key] ?? (await promptForSchema(rl, key, base));
        }
    }
    rl.close();
    return options;
}
//# sourceMappingURL=promptForPresetOptions.js.map
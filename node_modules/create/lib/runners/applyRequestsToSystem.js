export async function applyRequestsToSystem(requests, fetchers) {
    await Promise.all(requests.map(async (request) => {
        await request.send(fetchers);
    }));
}
//# sourceMappingURL=applyRequestsToSystem.js.map